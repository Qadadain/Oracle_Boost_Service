<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand d-flex align-items-center" href="{{ path('home') }}"> <img
                class="logo-oracle-nav" src="{{ asset('build/images/logo-oracle.png') }}" alt=""><p class="m-0 pl-1">Oracle Boost Service</p></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
            aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            {% if is_granted('ROLE_BOOSTER') or is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('dungeon_boost_show') }}">Boost MM+</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('character_index') }}">Boosteur</a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('raid_boost_show') }}">Boost Raid </a>
            </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin') }}">Admin</a>
                </li>
            {% endif %}
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button"
                       aria-haspopup="true" aria-expanded="false">Bonjour {{ app.user.pseudo }} ! </a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('profile_index') }}">Mon Profil</a>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button"
                       aria-haspopup="true" aria-expanded="false">Connexion / inscription</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('app_login') }}">Connexion</a>
                        <a class="dropdown-item" href="{{ path('app_register') }}">Inscription</a>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
